name: Deploy AI Orchestrator to EC2

on:
  push:
    branches:
      - main
    paths:
      - 'ai-orchestrator/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: Setup SSH key
      - name: Setup SSH key
        run: |
          set -e  # Exit on any error
          
          # Debug: Check home directory and permissions
          echo "Home directory: $HOME"
          echo "Current user: $(whoami)"
          echo "Listing home directory:"
          ls -la ~ || true
          
          # Check if .ssh exists and what type it is
          echo "Checking for existing .ssh..."
          if [ -e ~/.ssh ]; then
            echo ".ssh exists, checking type:"
            ls -ld ~/.ssh
            if [ -f ~/.ssh ]; then
              echo "ERROR: ~/.ssh is a file, not a directory. Removing it."
              rm -f ~/.ssh
            elif [ -d ~/.ssh ]; then
              echo ".ssh directory already exists"
            fi
          else
            echo ".ssh does not exist yet"
          fi
          
          # Create .ssh directory
          echo "Creating .ssh directory..."
          mkdir -p ~/.ssh
          echo "✓ .ssh directory created/verified"
          
          # Set proper permissions on .ssh directory
          echo "Setting permissions on .ssh directory..."
          chmod 700 ~/.ssh
          echo "✓ Permissions set"
          
          # Create SSH key file
          echo "Creating SSH key file..."
          echo "${{ secrets.EC2_SSH_KEY }}" | tr -d '\r' > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          echo "✓ SSH key created"
          
          # Add host to known_hosts
          echo "Adding host to known_hosts..."
          if ssh-keyscan -H 54.183.212.95 >> ~/.ssh/known_hosts 2>&1; then
            chmod 644 ~/.ssh/known_hosts
            echo "✓ Host added to known_hosts"
          else
            echo "WARNING: ssh-keyscan failed. This might indicate:"
            echo "  - EC2 instance is not running"
            echo "  - Security group doesn't allow SSH from GitHub Actions"
            echo "  - IP address is incorrect"
            echo "Attempting to continue anyway..."
            touch ~/.ssh/known_hosts
            chmod 644 ~/.ssh/known_hosts
          fi
          
          # Verify key format
          echo "Verifying SSH key format..."
          head -n 1 ~/.ssh/id_rsa
          tail -n 1 ~/.ssh/id_rsa
          echo "✓ SSH setup complete"

      # Step 3: Sync ai-orchestrator folder to EC2
      - name: Sync AI Orchestrator files to EC2
        run: |
          echo "Testing SSH connection to EC2..."
          if ssh -i ~/.ssh/id_rsa \
            -o StrictHostKeyChecking=no \
            -o ConnectTimeout=10 \
            -o BatchMode=yes \
            ubuntu@54.183.212.95 'echo "SSH connection successful"'; then
            echo "✓ SSH connection verified"
          else
            echo "ERROR: Cannot connect to EC2 instance"
            echo "Please check:"
            echo "  1. EC2 instance is running"
            echo "  2. Security group allows SSH from GitHub Actions"
            echo "  3. IP address 54.183.212.95 is correct"
            exit 1
          fi
          
          echo "Starting rsync..."
          rsync -avz --progress \
            --exclude '.git' \
            --exclude 'venv' \
            --exclude '__pycache__' \
            --exclude '.env' \
            -e "ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no -o ConnectTimeout=30" \
            ./ai-orchestrator/ ubuntu@54.183.212.95:/home/ubuntu/ai-orchestrator
          
          echo "✓ Files synced successfully"

      # Step 4: Setup virtual environment and install dependencies
      - name: Setup and Install Dependencies
        run: |
          ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no ubuntu@54.183.212.95 '
            set -e
            cd /home/ubuntu/ai-orchestrator

            # Create virtual environment if it does not exist
            if [ ! -d "venv" ]; then
              echo "Creating virtual environment..."
              python3 -m venv venv
            fi

            echo "Activating virtual environment..."
            source venv/bin/activate

            echo "Installing dependencies..."
            pip install --upgrade pip
            pip install --upgrade -r requirements.txt --no-input

            echo "Restarting service..."
            sudo systemctl restart ai-orchestrator
          '

      # Step 5: Health Check
      - name: Health Check
        run: |
          echo "Waiting for service to start..."
          sleep 10
          
          echo "Checking service status on EC2..."
          ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no ubuntu@54.183.212.95 '
            echo "=== Service Status ==="
            sudo systemctl status ai-orchestrator --no-pager || true
            echo ""
            echo "=== Recent Logs ==="
            sudo journalctl -u ai-orchestrator -n 50 --no-pager || true
            echo ""
            echo "=== Checking if port 8000 is listening ==="
            sudo netstat -tlnp | grep 8000 || echo "Port 8000 is not listening"
          '
          
          echo ""
          echo "Checking service health from GitHub Actions..."
          for i in {1..5}; do
            echo "Attempt $i of 5..."
            if curl -f --max-time 10 --connect-timeout 5 http://54.183.212.95:8000/health 2>&1; then
              echo "✅ Service is healthy!"
              exit 0
            fi
            echo "Health check failed, retrying in 5 seconds..."
            sleep 5
          done
          
          echo ""
          echo "❌ Service health check failed after 5 attempts"
          echo "This could mean:"
          echo "  1. Service failed to start (check logs above)"
          echo "  2. Service is not listening on port 8000"
          echo "  3. Security group doesn't allow HTTP traffic on port 8000 from GitHub Actions"
          echo ""
          echo "Checking final service status..."
          ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no ubuntu@54.183.212.95 'sudo systemctl status ai-orchestrator --no-pager'
          exit 1